<tr>
  <td style="padding: 10px 20px 20px; background:url('{{ url.imgPath }}/{{ movebox.img }}') no-repeat top right;">
    <h1 style="margin: 10px;">
        <font face="{{ font.face }}" color="{{ movebox.titleColor }}" style="font-weight: 200; font-size: 24px; letter-spacing: -1px;">{{ movebox.title }}</font>
      </h1>
    <table width="{{ movebox.width }}" cellspacing="0" align="{{ movebox.align }}">
      <tbody>
        <tr>
          <td style="background:none no-repeat left bottom scroll {{ movebox.bgColor }}; padding: {{ padding }}; border-radius: 30px 0 30px 0 ">
            <h4 style="font-weight:400; font-size: 24px; letter-spacing: -1px; margin: 0 0 10px;">
              <font color="{{ movebox.subtitleColor }}" face="{{ font.face }}">{{ movebox.subtitle }}</font>
            </h4>
            <font style="font-size: 18px;" face="{{ font.face }}" color="{{ colors.text }}">
              123 Fake St
              <br>Toronto, ON
              <br>M2M 2M2
            </font>
            <h4 style="font-size: 24px; letter-spacing: -1px; margin: 20px 0 10px 0; font-weight:400">
              <font color="{{ colors.text }}" face="{{ font.face }}">{{ movebox.details }}</font>
            </h4>
            <p style="margin:0; font-size: 18px; letter-spacing: 0px;">
              <font face="{{ font.face }}" color="{{ colors.text }}">
                October 12, 2016
              </font>
            </p>
          </td>
        </tr>
      </tbody>
    </table>
  </td>
</tr>