<tr>
  <td>
    <table width="{{ movebox.width }}" cellspacing="0" align="right">
      <tbody>
        <tr>
          <td style="background:none no-repeat left bottom scroll {{ movebox.bgColor }}; padding: {{ padding }}; border-radius: 30px 0 30px 0 ">
            <h4 style="font-weight:400; font-size: 24px; letter-spacing: -1px; margin: 0 0 10px;">
              <font color="{{ movebox.subtitleColor }}" face="{{ font.face }}">{{ movebox.right.subtitle }}</font>
            </h4>
            <font style="font-size: 18px;" face="{{ font.face }}" color="{{ colors.text }}">
              {{ movebox.right.content }}
            </font>
            <h4 style="font-size: 24px; letter-spacing: -1px; margin: 20px 0 10px 0; font-weight:400">
              <font color="{{ colors.text }}" face="{{ font.face }}">{{ movebox.right.details }}</font>
            </h4>
            <p style="margin:0; font-size: 18px; letter-spacing: 0px;">
              <font face="{{ font.face }}" color="{{ colors.text }}">
                {{ movebox.right.date }}
              </font>
            </p>
          </td>
        </tr>
      </tbody>
    </table>
  </td>
</tr>
